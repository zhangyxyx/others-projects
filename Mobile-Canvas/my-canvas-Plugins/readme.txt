这是用canvas实现画图形的一个jquery插件
目前代码都在js/main.js文件中，这个文件里面是有矩形、圆形、线段（不规则图形）
1.矩形
$('').rect(x,y,w,h,color)
2.圆形
$('').cEill(x,y,r,start,end,color)
3.线段
var json={
    m:{
        x:,y:
    },
    l:[
        {x:,y:}//如果只是线段就是结束
        {x:,y:}
        {x:,y:}//如果是不规则图形的话就需要多个点来确定连线（按数组顺序）
    ],
    color：//颜色
}
$('').path()
